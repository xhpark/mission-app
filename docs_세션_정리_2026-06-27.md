# 세션 정리 — 2026-06-27 (학습자 롤아웃 운영 단계)

> 코드 변경은 없는 세션이었음(검증/운영 작업 위주). 마지막 코드 커밋은 `cec3619`
> (학습자 명단 관리 기능)이며, 이 문서 작성 시점까지 그 이후 추가 커밋 없음.

---

## 1. 현재 배포 상태 (요약)

- **배포 채널**: Firebase Storage 공개 경로
  `https://firebasestorage.googleapis.com/v0/b/mission-app-b29ed.firebasestorage.app/o/app_releases%2Fmission_app.apk?alt=media`
  (release 서명 APK, `scripts/upload_app_release.py`로 업로드)
- **학습자 사용설명서**: `docs_학습자_사용설명서_2026-06-26.docx`
- **배포 설명회 자료(사전 준비 체크리스트 포함)**: `docs_배포_설명회_자료_2026-06-26.pptx`
- **학습자 명단(roster) 34명**: `학습자 명단 관리` 화면(관리자 메뉴)에 일괄 등록 완료
  (원본 `D:\문서\태국선교\언어\전화번호.txt`, 19번 "이경자 권사" 전화번호 오타
  `0010-7340-8606` → `010-7340-8606` 정정해서 등록함).
- **백엔드(Cloud Functions, Firestore rules)**: 모두 배포 완료 + IAM 검증(23/23 OK) 완료.

---

## 2. 이번 세션에서 확인/처리한 것

1. **실기기 테스트**: 테스트 폰(`R3CX60PDSTA`)에 debug 빌드 설치 후 학습 선택 화면 →
   학습자 명단 관리 화면까지 정상 동작 확인(스크린샷으로 검증).
   - ⚠️ release APK는 이미 폰에 깔려있던 debug 서명과 달라 `adb install -r`로 덮어쓰기
     실패(`INSTALL_FAILED_UPDATE_INCOMPATIBLE`) → debug APK로 설치해 로그인 세션 유지.
2. **온디바이스(폰 전용) ASR 모델 동작 확인**:
   - `isModelInstalled()`([on_device_asr_model_downloader.dart:78-86](lib/core/services/on_device_asr_model_downloader.dart#L78-L86))은
     SharedPreferences 같은 "설치 기록 플래그"가 아니라 **실제 파일 존재 여부**를
     매번 직접 검사함.
   - 테스트 폰에는 이전 세션의 ASR 테스트 때문에 모델이 이미 설치돼 있어 다운로드
     다이얼로그가 안 보였던 것 — 정상 동작이었음. 확인을 위해 모델 폴더
     (`/storage/emulated/0/Android/data/com.thaimission.app/files/sherpa-onnx`)를
     adb로 삭제함 → 다음 "폰 전용" 선택 시 다운로드 다이얼로그가 다시 뜰 것임(미검증,
     다음 세션에서 실제로 떠보는지 확인 필요).
3. **QA 계정 안내 문구 노출 문의**: [login_screen.dart:195-198](lib/features/auth/presentation/screens/login_screen.dart#L195-L198)의
   QA 계정 힌트는 `kDebugMode` 조건부라 release 빌드(학습자 배포용)에는 절대 노출되지
   않음을 코드로 확인. 테스트 폰에 debug 빌드가 깔려 있어서 보였던 것일 뿐, 실제
   배포물은 문제 없음.
4. **서버 음성인식(STT) 비용 추정**: Google Cloud Speech-to-Text v2가 유일한 의미있는
   변동비. 34명 규모에서 월 $54~144(약 7~20만 원) 수준으로 추정(가정 기반 추정치,
   실측은 GCP 빌링 콘솔에서 1주 운영 후 확인 권장). 다른 항목(Functions, Firestore,
   Storage 용량, 이메일, 텔레그램, Auth)은 이 규모에서는 무료 한도 안.
5. **학습자 계정 1명 삭제 처리**: 관리자가 직접 Firebase Console에서
   - Authentication → 해당 이메일 계정 삭제
   - Firestore → `user_profiles/{uid}` 문서 삭제
   를 수행해서 완료. 명단(roster)에는 자동으로 "미가입"으로 다시 표시될 것(명단
   자체에서도 빼려면 `학습자 명단 관리` 화면의 휴지통 아이콘 사용).

---

## 3. 운영 중 알아두면 좋은 사실 (재배포 불필요)

| 하고 싶은 일 | 방법 | 재배포 필요? |
|---|---|---|
| 학습자 사전 명단에서 1명 제거 | 학습자 명단 관리 화면 → 휴지통 아이콘 | ❌ |
| 가입 완료된 계정 일시 차단(되돌리기 쉬움) | Firestore Console → `user_profiles/{uid}.status`를 `"blocked"`로 변경 | ❌ |
| 가입 완료된 계정 영구 삭제 | Firebase Console Authentication에서 계정 삭제 + Firestore에서 `user_profiles/{uid}` 문서 삭제 | ❌ |
| 오늘의 학습/복습 링크 등록 | 관리자 대시보드 내 입력 폼 | ❌ |

Firestore: https://console.firebase.google.com/project/mission-app-b29ed/firestore/data
Authentication: https://console.firebase.google.com/project/mission-app-b29ed/authentication/users

---

## 4. 다음 세션에서 이어갈 만한 것 (보완 개발 후보, 우선순위 순)

1. **관리자 UI에 "차단/삭제" 버튼 추가** (현재 미구현, Firebase Console을 직접 열어야
   함 — 매번 콘솔 들어가는 게 번거롭고 실수 위험도 있음). 관리자 대시보드 또는
   학습자 명단 화면에 `blockLearnerAccount`/`deleteLearnerAccount` 콜러블 + 확인
   버튼을 추가하면 운영이 훨씬 편해짐.
2. **온디바이스 ASR 다운로드 플로우 실제 검증**: 모델 폴더를 지운 상태에서 "폰 전용"을
   선택했을 때 다운로드 다이얼로그 → 진행률 → 완료까지 실제로 한 번 끝까지 테스트
   필요(이번 세션엔 폴더 삭제까지만 하고 미검증).
2-1. *(주의)* 위 검증을 adb로 자동화할 때 `input swipe`는 쓰지 말 것 — 이전 세션에서
   스와이프 제스처가 카카오톡 같은 무관한 앱으로 잘못 들어간 적 있음(읽지 않고 즉시
   복귀했지만 재발 방지 필요). `input tap` + 스크린샷 확인 위주로.
3. **전화번호 인증 전환은 여전히 보류 상태** — `docs_phone_auth_migration_analysis_2026-06-24.md`
   참고. 착수 전 Phase 0 결정(관리자 식별 방식, 이메일 병행 여부, SMS 사업자) 먼저
   필요. 관리자 본인 락아웃 리스크가 1순위 위험.
4. **STT 비용 실측치 확인**: 1주 정도 실사용 데이터가 쌓이면 GCP 빌링 콘솔에서
   Speech-to-Text 실제 청구액을 확인해서 이 문서의 추정치(월 $54~144)와 비교.
5. 이번 세션에서 지운 학습자의 과거 `study_sessions`/`reports` 등 흔적 데이터 정리
   여부는 보류 상태 — 통계에 영향 없는 수준이라 급하지 않음, 필요시 처리.

---

## 5. 참고 문서 인덱스

- `docs_학습자_사용설명서_2026-06-26.docx` — 학습자용 사용 설명서(Word)
- `docs_배포_설명회_자료_2026-06-26.pptx` — 배포 설명회용 발표자료 + 사전 준비 체크리스트
- `docs_phone_auth_migration_analysis_2026-06-24.md` — 전화번호 인증 전환 영향 분석(구현 보류)
- `docs_function_deploy_checklist_2026-06-23.md` — Cloud Functions 배포 후 IAM 점검 체크리스트
