# 운영 종료 정리 — 2026-08-25 (선교 종료 후 마무리)

> 선교 일정이 끝난 뒤의 마무리 세션. 미커밋 상태로 남아 있던 운영 코드를 커밋·푸시하고,
> 스케줄 작업을 멈추고, 실제 비용을 확인하고, 최종 백업을 받고, 작업 잔여물을 정리했다.
> 이전 세션 기록은 `docs_세션_정리_2026-06-27.md`.

---

## 1. 이번에 처리한 것

### 1-1. 미커밋 운영 코드 커밋 + 푸시

마지막 커밋(`1a3d158`, 2026-06-28) 이후 **두 달간 커밋되지 않은 채 워킹트리에만 있던
2,008줄**을 커밋했다. 이 코드는 **이미 프로덕션에 배포돼 돌아가고 있었다** — 즉 운영 중인
서버 코드가 git 히스토리에 없는 상태였다.

| 커밋 | 내용 |
|---|---|
| `47debfb` | 유사도 이벤트 + 일일 활동 리포트 (2,008줄, functions/rules/indexes/admin UI) |
| `0ceae61` | 학습자 사용설명서 docx 갱신 (7/01자 수정분) |
| `c12b0ab` | `.gitignore` 보강 (아래 1-2 참고) |

푸시 완료: `cec3619..c12b0ab` → `origin/main`. 커밋 전 검증 — `tsc --noEmit` 0 errors,
`flutter analyze` No issues found.

커밋한 기능의 실제 내용:
- **유사도 이벤트**: `startSimilarityEvent` / `endSimilarityEvent` /
  `getSimilarityEventDashboard` 콜러블, 리포트 항목에 `similarityEventId`·
  `similarityEventReceptionId` 태깅, 관리자 대시보드 랭킹 UI(개인/평균/리포트별),
  두 필드에 대한 Firestore 단일 필드 인덱스
- **일일 활동 리포트**: 06:20 KST 스케줄 함수가 `daily_activity_reports/{dateKey}`를
  쓰고 텔레그램 요약 발송, `getDailyActivityReport`·`generateDailyActivityReportForDate`
  콜러블(조회/특정일 재생성), 관리자 대시보드에 상위 학습자·미활동 학습자 섹션,
  `daily_activity_reports`는 서버 전용으로 rules 잠금
- 리포트 시각을 UTC로 저장하고 KST로 명시 표기하도록 변경 — 일일 리포트의 날짜 구간과
  학습자가 보는 시각을 일치시키기 위함

### 1-2. 리포에 방치돼 있던 계정 덤프 처리 (⚠️ 사고 직전이었음)

리포 루트에 **`-`라는 이름의 파일**이 untracked로 있었다. 내용은
`firebase auth:export` 결과 — **학습자 37명의 이메일 + 비밀번호 해시 + salt**.
리다이렉션 실수로 생긴 것으로 보인다(7/18자).

`.gitignore`에 걸려 있지 않아서 `git add .` 한 번이면 **공개 GitHub 리포에 그대로
올라갈 상태**였다. 리포 밖으로 옮기고(`D:\backup\mission_app_2026-08-25\`),
같은 실수가 반복되지 않도록 패턴을 `.gitignore`에 등록했다
(`firebase_auth_export*.json`, `auth_export*.json`, `/-`).

### 1-3. 일일 리포트 스케줄 중지

`generateDailyActivityReport`가 `20 6 * * *` (Asia/Seoul)로 계속 살아 있어서 선교가
끝난 뒤에도 매일 아침 텔레그램 리포트를 보내고 있었다.

Cloud Scheduler 작업을 **일시중지(PAUSED)** 했다. 함수 자체는 배포된 채로 남겨 뒀다 —
호출이 없으면 비용이 발생하지 않고, 되돌리기가 쉽기 때문이다.

```bash
# 현재 상태 확인
gcloud scheduler jobs describe firebase-schedule-generateDailyActivityReport-asia-northeast3 \
  --project mission-app-b29ed --location asia-northeast3 --format="value(state)"

# 다시 켜고 싶을 때
gcloud scheduler jobs resume firebase-schedule-generateDailyActivityReport-asia-northeast3 \
  --project mission-app-b29ed --location asia-northeast3
```

### 1-4. 잔여물 정리

`tmp_*.png` 57개, `tmp_test_logs/`, `functions/build/`를 삭제하고 `.gitignore`에 등록했다.

---

## 2. 실제 운영 결과 — STT 비용은 추정의 약 1/25였다

`docs_세션_정리_2026-06-27.md`의 추정치는 **월 $54~144**였다. Cloud Monitoring의
`serviceruntime.googleapis.com/api/request_count` (service = `speech.googleapis.com`)
실측은 다음과 같다.

| 월 | 요청 수 | 분(요청당 15초 가정) | 과금분(무료 60분 차감) | 비용(@$0.016/분) |
|---|---:|---:|---:|---:|
| 2026-05 | 16 | 4.0 | 0.0 | $0.00 |
| 2026-06 | 59 | 14.8 | 0.0 | $0.00 |
| 2026-07 | 1,597 | 399.2 | 339.2 | $5.43 |
| **합계** | **1,672** | | | **약 $5.43** |

- 트래픽 구간: **2026-05-23 ~ 2026-07-24**. 최다 사용일은 7/17(521건), 7/19(367건),
  7/20(280건) — 선교 기간에 집중됐다.
- **2026-07-24 이후 STT 호출이 0건**이다. 앱은 한 달째 사실상 유휴 상태.

**추정이 왜 20배 이상 빗나갔나**: 34명이 매일 말하기 연습을 한다는 가정으로 계산했으나,
실제로는 사용이 특정 며칠에 몰렸고 전체 요청 수 자체가 훨씬 적었다.

> ⚠️ 이 표는 **사용량에서 역산한 추정**이지 청구서가 아니다. STT는 15초 단위로 올림
> 과금되므로, 요청이 모두 15초 미만인 짧은 발화라면 위 값이 상한에 가깝다. 확정 금액은
> GCP 빌링 콘솔에서 확인해야 한다(이 프로젝트에는 BigQuery 빌링 export가 설정돼 있지
> 않아 CLI로는 실제 청구액을 조회할 수 없다).
> https://console.cloud.google.com/billing/01AAAB-20F222-2791F0/reports?project=mission-app-b29ed

---

## 3. 최종 백업 — `D:\backup\mission_app_2026-08-25\`

390개 파일, 181 MB. 상세는 그 폴더의 `README_BACKUP.txt` 참고.

| 항목 | 내용 |
|---|---|
| `firestore_export/` | Firestore 전체 export (9.34 MiB, 2026-08-25, 상태 SUCCESSFUL) |
| `storage/speaking_attempts/` | 학습자 발화 녹음 24.85 MiB — **대체 불가능한 유일한 데이터** |
| `storage/audio/` | TTS 캐시 605 KiB (재생성 가능) |
| `storage/app_releases/` | 배포한 release APK 144.69 MiB (2026-06-26자) |
| `firebase_auth_export_2026-08-25.json` | 계정 37개 (이메일 + 비밀번호 해시 + salt) |
| `firebase_auth_export_2026-07-18.json` | 1-2에서 리포 밖으로 옮긴 이전 덤프 |

받지 않은 것: `storage/asr_models/` (149.78 MiB) — sherpa-onnx 공개 모델이라 원본에서
다시 받으면 된다.

**주의 사항 두 가지**
- auth export 파일에는 비밀번호 해시와 salt가 들어 있다. **git 리포에 넣지 말 것.**
- `speaking_attempts/.../dev-local-2026-04-27T06-09-38.664` 폴더는 원본 이름의
  콜론(`:`)을 하이픈으로 바꾼 것이다(Windows 파일명 제약). 원본 GCS 경로는 `06:09:38.664`.

Firestore 백업은 GCS에도 남아 있다: `gs://mission-app-b29ed-backup-20260825`
(export를 위해 이번에 만든 US 멀티리전 버킷 — 기존 Storage 버킷이 us-west1이라
nam5 데이터베이스의 export 대상이 될 수 없었다).

복원이 필요하면:
```bash
gcloud firestore import gs://mission-app-b29ed-backup-20260825/firestore_2026-08-25 \
  --project mission-app-b29ed
firebase auth:import firebase_auth_export_2026-08-25.json --hash-algo=SCRYPT ... \
  --project mission-app-b29ed   # 해시 파라미터는 Firebase Console > Auth > 사용자 가져오기 참고
```

---

## 4. 아직 하지 않은 것 — 서비스 실제 철거는 보류

백업까지는 끝냈지만 **계정·서비스 삭제는 실행하지 않았다.** 되돌릴 수 없고 학습자 34명에게
영향이 가는데, 어디까지 지울지가 정해지지 않았기 때문이다. 선택지는 대략 세 가지다.

| 범위 | 내용 | 되돌리기 |
|---|---|---|
| **A. 최소** | 스케줄만 멈춘 현 상태 유지. 앱·계정 그대로, 학습자가 원하면 계속 사용 가능 | — (이미 적용됨) |
| **B. 배포 중단** | Storage의 `app_releases/mission_app.apk` 삭제 → 신규 설치·재설치 차단. 기존 설치분은 계속 동작 | APK 백업본으로 재업로드 가능 |
| **C. 전체 철거** | Auth 계정 37개 + Firestore 데이터 삭제, Functions 삭제, 최종적으로 Firebase 프로젝트 삭제 | **불가** (백업에서 재구축만 가능) |

C를 택하더라도 순서가 중요하다 — **학습자에게 종료를 먼저 공지**하고, 백업 무결성을 한 번
더 확인한 뒤, 프로젝트 삭제는 마지막에. 프로젝트를 지우면 위 GCS 백업 버킷도 함께 사라지므로
로컬 백업(`D:\backup\mission_app_2026-08-25\`)이 유일본이 된다.

참고로 현 상태(A)의 유지 비용은 사실상 0에 가깝다 — 호출이 없어 Functions·STT 비용이
발생하지 않고, 남는 것은 Storage 320 MiB와 Firestore 저장 용량뿐으로 무료 한도 안이다.
서두를 이유는 없다.

---

## 5. 그 밖에 남아 있는 것

- **`.agents/`, `.codex/`** — untracked 상태로 남겨 뒀다. 에이전트 설정이라 정리 대상인지
  판단이 필요하다(`.agents/skills/content-update/SKILL.md`는 `.claude/skills` 쪽과 중복
  가능성 있음).
- **`android/app/src/main/AndroidManifest.xml`** — 내용 변경 없이 줄바꿈(LF/CRLF)만 다른
  상태라 커밋하지 않았다. 무해하다.
- 이전 세션에서 넘어온 미완 항목(운영을 재개할 경우에만 의미 있음):
  온디바이스 ASR 다운로드 플로우 실검증, 관리자 차단/삭제 버튼(현재 Firebase Console 수동),
  전화번호 인증 전환(`docs_phone_auth_migration_analysis_2026-06-24.md`, 보류 유지).

---

## 6. 참고 문서 인덱스

- `docs_세션_정리_2026-06-27.md` — 직전 세션(학습자 롤아웃 운영 단계) 기록
- `docs_학습자_사용설명서_2026-06-26.docx` — 학습자용 사용 설명서
- `docs_배포_설명회_자료_2026-06-26.pptx` — 배포 설명회 발표자료
- `docs_phone_auth_migration_analysis_2026-06-24.md` — 전화번호 인증 전환 영향 분석(보류)
- `docs_function_deploy_checklist_2026-06-23.md` — Functions 배포 후 IAM 점검 체크리스트
- `D:\backup\mission_app_2026-08-25\README_BACKUP.txt` — 백업 구성과 복원 방법
